import React from "react";
import EditorLayout from "./EditorLayout";

function ResumeEditor({resumeID, formData}:any) {
  // console.log(JSON.stringify(formData))
  return <EditorLayout />;
}

export default ResumeEditor;
